# TODO — за пределами v1

## Из мастер-наряда (2026-07-16) — то, чего ещё нет
- **Вариант B — мозг на подписке (Hetzner).** Мини-эндпоинт за туннелем дёргает
  `claude -p --output-format json`; токены идут из подписки Claude Code, а не из
  API-биллинга. Фронт не меняется — только URL в `/api/chat`. Требует работающего
  сервера. ⚠️ Точка «стоп + ОК Игоря» (трата/сервер). Запрещено выдёргивать
  OAuth-токен подписки в чужие прокси (ToS).
- **Spend-cap в консоли Anthropic** — биллинговый предохранитель на стороне
  Anthropic поверх нашего программного лимитера (`DAILY_COST_LIMIT` в chat.js).
  Ручной шаг Игоря, 2 минуты.

## Прочее
- Жёсткий rate-limit во внешнем сторе (Netlify Blobs / Upstash) — сейчас лимит в памяти функции, обнуляется холодным стартом.
- Wake-word («Таймень!») поверх постоянного прослушивания.
- Стриминг ответа (SSE через функцию) — сейчас ответ приходит целиком.
- Whisper-STT на сервере для браузеров без SpeechRecognition (Firefox).
- Параллельные суб-агенты: сущности отвечают одновременно и совещаются (сейчас — по одному, экономия токенов).
- Скармливание PDF (pdf.js) и картинок (vision через прокси).

## Уже сделано (было в TODO)
- ~~ElevenLabs-голос через tts.js + переключатель~~ — есть, по-планетно тембры.
- ~~Пароль на доступ~~ — есть `ACCESS_KEY`.
- ~~Память в git, выгрузка знаний~~ — есть (ветка taimen-memory, memory.js).
